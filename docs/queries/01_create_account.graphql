mutation {
  createLedgerAccount(input: { name: "test1", kind: ASSET, isGroup: false }) {
    id
    parent {
      name
    }
    name
    kind
    isGroup
    archivedAt
    createdAt
    updatedAt
  }
}
